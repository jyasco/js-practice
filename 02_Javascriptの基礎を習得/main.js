'use strict';

// 配列の場合
const colors = ['Red', 'Green', 'Blue'];
for(const color of colors) {
  console.log(color);
}

// 文字列の場合
const str = 'JavaScript';
for(const value of str){
  console.log(value);
}